# Local development (npm run dev)
VITE_API_URL=http://localhost:8001

# Production (Vercel) — also set in Vercel dashboard → Settings → Environment Variables
# VITE_API_URL=https://career-verse-smra.onrender.com
